"""Synthèse périodique et mensuelle des flux réconciliés."""

import pandas as pd


def generate_monthly_summary(df_reconciled: pd.DataFrame) -> pd.DataFrame:
    """Agrège les montants rapprochés par mois et par statut."""
    return pd.DataFrame()
