"""Analyse approfondie des transactions suspectées de doublons."""

import pandas as pd


def analyze_duplicates(duplicates_df: pd.DataFrame) -> pd.DataFrame:
    """Regroupe et commente les doublons trouvés pour validation humaine."""
    return duplicates_df
